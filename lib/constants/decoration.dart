import 'package:flutter/material.dart';

class Decorations {
  static TextStyle hintTextStye = TextStyle(color: Colors.grey);
  static BorderRadius inputsBorderRadius = BorderRadius.circular(12);
  static BorderRadius loginButtonRadius = BorderRadius.circular(5);
  static BorderRadius kategoriBorderRadius = BorderRadius.circular(20);
}
